# Mihomo output

The first artifact layer produces validated combined IPv4/IPv6 data and a simple Mihomo-compatible payload.

Native Mihomo rule-providers integration will be added after the source format is verified against the current CDN-Cloud manifest.
